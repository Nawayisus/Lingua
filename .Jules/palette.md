## 2026-02-19 - [Bootstrap Accessibility Gaps]
**Learning:** Standard Bootstrap form labels in this project lack automatic `for` association, and progress bars miss `aria-valuenow` updates.
**Action:** Always manually add `for` attributes to labels and ensure JS updates `aria-valuenow` for dynamic progress bars.
