# Palette Journal

## 2024-05-22 - Accessible Custom File Upload
**Learning:** Wrapping custom file upload areas in a `<label>` with a `visually-hidden` input ensures native clickability and keyboard accessibility without complex JS.
**Action:** Use this pattern for all future custom inputs to leverage `:focus-within` for focus styles.
