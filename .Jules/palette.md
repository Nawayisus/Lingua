## 2024-05-22 - Form Accessibility Basics
**Learning:** The application heavily relies on standard Bootstrap forms but neglected basic HTML association (for/id) and ARIA attributes for dynamic elements like progress bars.
**Action:** Always verify `for` attributes on labels and ARIA attributes on dynamic components during any UI work.
